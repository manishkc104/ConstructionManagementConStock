@model IEnumerable<ADGroup4L3C3.Models.StockManagment>

@{
    ViewBag.Title = "Index";
}
@section content{
<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Operation Type
        </th>
        <th>
            Operation Date
        </th>
        <th>
            Quantity
        </th>
        <th>
            Date Created
        </th>
        <th>
            Date Modified
        </th>
        <th>
            Stock ID
        </th>
        <th>
           Modified By
        </th>
        <th>
            User
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OperationType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OperationDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Quantity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateCreated)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateModified)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Stock.StockID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserAccounts.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserAccounts1.Email)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StockManagementID }) |
            @Html.ActionLink("Details", "Details", new { id=item.StockManagementID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StockManagementID })
        </td>
    </tr>
}

</table>
}